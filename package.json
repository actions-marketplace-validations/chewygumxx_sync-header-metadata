{
  "name": "sync-header-metadata",
  "description": "[GitHub Action] Checks and updates the \"~owner/repo.git\" and \"::: :/<path>\" header lines in tracked files.",
  "version": "2.0.0",
  "license": "GPL-3.0-only",
  "keywords": [
    "github-actions",
    "repository",
    "metadata"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "prepare": "test -d node_modules/husky && husky || true",
    "test": "node --test"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "globals": "^17.12.0",
    "husky": "^9.1.7"
  }
}
