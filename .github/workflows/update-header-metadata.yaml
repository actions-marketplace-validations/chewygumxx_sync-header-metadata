# vim:set expandtab shiftwidth=4 filetype=yaml foldlevel=5:
# SPDX-License-Identifier: GPL-3.0-only

#
#
# ~chewygumxx/sync-header-metadata.git
# ::: :/.github/workflows/update-header-metadata.yaml
#
#

name: Update Header Metadata

on:
    push:
        branches:
            - main

jobs:
    update:
        runs-on: ubuntu-latest

        permissions:
            contents: write

        steps:
            - uses: actions/checkout@v7
              with:
                  ref: ${{ github.head_ref || github.ref_name }}
                  persist-credentials: true

            - name: Update tracked files
              uses: chewygumxx/sync-header-metadata@v2
              with:
                  mode:        update
                  verbose:    'false'
                  annotation: 'true'

            - name: Commit changes
              uses: stefanzweifel/git-auto-commit-action@v7
              with:
                  commit_message: "chore: Sync header metadata"
